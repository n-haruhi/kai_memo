<div class="container">
  <div class="col">
    <h1>タスク詳細</h1>

    <table class="table">
      <tr>
        <td><%= @task.title %></td>
        <td><%= link_to '編集', edit_task_path, class: "btn btn-sm btn-success" %></td>
        <td><%= link_to '削除', @task, method: :delete, data: { confirm: '本当に消しますか？' }, class: "btn btn-sm btn-danger"%></td>
      </tr>
    </table>
  </div>
</div>