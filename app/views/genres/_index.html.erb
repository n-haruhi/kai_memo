<div class="col">
  <table class="table">
    <thead>
      <tr>
        <th>ジャンル名</th>
        <th></th>
        </tr>
    </thead>
    <tbody>
      <% @genres.each do |genre| %>
        <tr>
          <td><%= genre.name %></td>
          <td class="text-right"><%= link_to "編集", edit_genre_path(genre), class: "btn btn-success" %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>